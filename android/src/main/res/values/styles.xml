<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             Flutter draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.
         
         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <style name="DemoLabel">
        <item name="android:gravity">left|center_vertical</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textSize">@dimen/demo_font_size</item>
        <item name="android:paddingTop">@dimen/demo_label_padding</item>
        <item name="android:paddingBottom">@dimen/demo_label_padding</item>
    </style>

    <style name="DemoEditText">
        <item name="android:textSize">@dimen/demo_font_size</item>
        <item name="android:singleLine">true</item>
    </style>
    <style name="MyTransparent">
        <item name="android:windowBackground">@color/colorTransparent</item> <!-- Background color transparency -->
        <item name="android:windowNoTitle">true</item><!-- No title -->
        <item name="android:windowIsTranslucent">true</item><!-- Translucent, set to false without transparency effect -->
        <item name="android:backgroundDimEnabled">true</item><!-- fuzzy -->
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item> <!-- Window style Dialog -->
    </style>
</resources>
